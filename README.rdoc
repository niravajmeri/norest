== INSTALLATION INSTRUCTIONS

1. Update database privileges in config/database.yml

2. <tt>rake db:create</tt> to create database

3. <tt>rake db:migrate</tt> to create tables

4. <tt>bundle install</tt> to install required gems

5. <tt>rails s </tt> to start the server

6. Open <tt>http://localhost:3000</tt> in the browser